package interedu.util;

/* 
 Implementation of priority Queue. 
 Author : Aravind Ragipindi. 
 found at: http://www.scribd.com/
 Re-Designed: Thomas Kurz
 */


import java.util.ArrayList;

public class FacetHeap {
//	
//	private ArrayList<SearchFacet> myHeap;
//
//	public FacetHeap() {
//		myHeap = new ArrayList<SearchFacet>();
//	}
//
//	/*
//	 * adds the given element to the priority queue.
//	 */
//	public void add(SearchFacet obj) {
//		myHeap.add(obj);
//		SearchFacet tmpObj, lastObj = obj;
//		int heapLastIndex = myHeap.size() - 1;
//		int lastParent = getParentIndex(heapLastIndex);
//		int i, j = heapLastIndex;
//		for (i = lastParent; i >= 0 && i != j; i = getParentIndex(i)) {
//			if (lastObj.compareTo(myHeap.get(i)) > 0) {
//				tmpObj = myHeap.get(i);
//				myHeap.set(i, lastObj);
//				myHeap.set(j, tmpObj);
//				j = i;
//			} else {
//				break;
//			}
//		}
//	}
//
//	/*
//	 * returns the parent node's index for any given index. We could check for
//	 * negative values here before rerturning.
//	 */
//	private int getParentIndex(int index) {
//		return (index % 2 == 0) ? (index - 2) / 2 : (index - 1) / 2;
//	}
//
//
//	/*
//	 * removeTop(); removes and returns the top Node.
//	 */
//	public SearchFacet removeTop() {
//		SearchFacet retObj = null;
//		if (!myHeap.isEmpty()) {
//			retObj = myHeap.get(0);
//			SearchFacet lastObj = myHeap.remove(myHeap.size() - 1);
//			if (!myHeap.isEmpty()) {
//				myHeap.set(0, lastObj);
//			}
//			int i = 0;
//			while (i < myHeap.size()) {
//				int maxIndex = getMaxIndex(2 * i + 1, 2 * i + 2);
//				if (maxIndex > 0 && lastObj.compareTo(myHeap.get(maxIndex)) < 0) {
//					SearchFacet tmpObj = myHeap.get(maxIndex);
//					myHeap.set(maxIndex, lastObj);
//					myHeap.set(i, tmpObj);
//					i = maxIndex;
//					continue;
//				}
//				break;
//			}
//		}
//		return retObj;
//	}
//
//	public boolean isEmpty() {
//		return myHeap.isEmpty();
//	}
//
//	/*
//	 * private : returns the index value for which the array is holding a higher
//	 * value among the two given indices.
//	 */
//	private int getMaxIndex(int x, int y) {
//		int retIndex = -1;
//		if (x < myHeap.size() && y < myHeap.size()) {
//			if ((myHeap.get(x)).compareTo( myHeap.get(y)) > 0) {
//				retIndex = x;
//			} else {
//				retIndex = y;
//			}
//		} else if (x >= myHeap.size() && y < myHeap.size()) {
//			retIndex = y;
//		} else if (x < myHeap.size() && y >= myHeap.size()) {
//			retIndex = x;
//		}
//		return retIndex;
//	}

}
