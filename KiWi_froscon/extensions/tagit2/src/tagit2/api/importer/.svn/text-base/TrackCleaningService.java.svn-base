package tagit2.api.importer;

import tagit2.util.parser.gpx.Track;

/**
 * a trackcleaner reduces the number of trackpoints. There are several possibilities
 * to do so
 * @author tkurz
 *
 */
public interface TrackCleaningService extends TrackCleaningServiceLocal,TrackCleaningServiceRemote {
	
	public Track reduceTracklistByNumber(Track track, int tracklistSize);

}
